# 처음 설치하는 분을 위한 안내

폰 하나로 처음부터 끝까지 따라 할 수 있게 적었습니다.
**위에서부터 순서대로** 하시면 됩니다. 중간에 건너뛰면 다음 단계에서 막힙니다.

이 앱은 스토어에 올라간 앱이 아니라 파일(APK)로 직접 설치하는 앱입니다.
그래서 안드로이드가 중간중간 "정말 설치할 거냐"고 여러 번 물어봅니다.
**앱이 잘못된 게 아니라 원래 그렇습니다.** 하나씩 풀어주면 됩니다.

> **준비물**: 안드로이드 8.0 이상 스마트폰. 아이폰에서는 쓸 수 없습니다.

전체 과정은 **1번부터 8번까지, 약 5분** 걸립니다.

---

## 1. 파일 내려받기

폰의 **Chrome**(또는 삼성 인터넷)으로 아래 주소를 엽니다.

```
github.com/KwanghoJung01/Auto-scroll/releases/tag/latest
```

1. 화면을 아래로 내려 **Assets** 를 찾습니다. (접혀 있으면 ▶ 를 눌러 펼칩니다)
2. 파일이 네 개 보입니다. 그중 **`AutoScroll-release.apk`** 를 누릅니다.

| 파일 | 설명 |
| --- | --- |
| **AutoScroll-release.apk** | ← **이걸 받으세요** |
| AutoScroll-debug.apk | 개발용. 받지 않아도 됩니다 |
| Source code (zip / tar.gz) | 소스 코드. 받지 않아도 됩니다 |

---

## 2. Chrome 경고 넘기기

파일을 누르면 아래쪽에 이런 문구가 뜹니다.

> **"이런 유형의 파일은 기기에 해로울 수 있습니다"**

**계속** 또는 **확인** 을 누르면 내려받아집니다.
(기기에 따라 *"파일 유지"*, *"그래도 다운로드"* 로 나오기도 합니다)

다 받으면 화면 아래나 알림창에 **AutoScroll-release.apk** 가 뜹니다. 그걸 누릅니다.

### 만약 다운로드가 아예 안 된다면

Chrome이 다운로드를 막고 있는 경우입니다.

1. Chrome 오른쪽 위 **⋮** → **다운로드**
2. 목록에서 파일을 찾아 다시 누릅니다

그래도 안 되면 **삼성 인터넷** 같은 다른 브라우저로 같은 주소를 열어 보세요.

---

## 3. "출처를 알 수 없는 앱" 허용하기

파일을 눌렀을 때 이런 창이 뜹니다.

> **"보안상의 이유로 이 출처의 알 수 없는 앱을 설치할 수 없습니다"**

창에 있는 **설정** 을 누르면 바로 해당 화면으로 갑니다. 거기서 스위치를 **켜기** 로 바꾸고 **뒤로** 돌아오면 됩니다.

창이 안 뜨거나 직접 찾아가고 싶다면:

- **삼성 갤럭시**: 설정 → 보안 및 개인정보 보호 → 기타 보안 설정 → **알 수 없는 앱 설치** → **Chrome** → 켜기
- **그 외 안드로이드**: 설정 → 앱 → 특별한 앱 액세스 → **알 수 없는 앱 설치** → **Chrome** → 켜기

> 여기서 켜는 건 "Chrome이 앱을 설치해도 된다"는 허락입니다. Chrome으로 받은 파일만 해당됩니다.

---

## 4. Google Play 프로텍트 차단 풀기

이 앱은 **접근성 기능**(화면을 대신 쓸어주는 기능)을 씁니다.
그런 앱은 스토어 밖에서 설치하면 구글이 자동으로 막습니다. 이런 창이 뜹니다.

> 🛡️ **"기기 보호를 위해 앱 차단됨"**
> "이 앱에서 민감한 정보에 대한 액세스 권한을 요청할 수 있습니다"

**확인** 밖에 없어서 그냥은 못 넘어갑니다. 이렇게 푸세요.

1. **Play 스토어** 앱을 엽니다
2. 오른쪽 위 **프로필 아이콘**(동그란 사진)을 누릅니다
3. **Play 프로텍트** 를 누릅니다
4. 오른쪽 위 **⚙️ 설정**(톱니바퀴)을 누릅니다
5. **Play 프로텍트로 앱 스캔** 을 **끕니다**
6. (같이 있으면) **유해한 앱 감지 개선** 도 끕니다

그리고 **2번으로 돌아가 APK를 다시 누르면** 이제 설치됩니다.

> ⚠️ **설치가 끝나면 Play 프로텍트를 다시 켜 두세요.**
> 다시 켠 뒤 "이 앱을 삭제하라"는 알림이 뜰 수 있는데, 무시하면 계속 쓸 수 있습니다.

### 삼성 갤럭시라면 "자동 차단"도 확인

삼성폰에는 **자동 차단(Auto Blocker)** 이라는 별도 기능이 하나 더 있습니다.
Play 프로텍트와 **다른 기능이라 둘 다 꺼야** 합니다.

설정 → 보안 및 개인정보 보호 → **자동 차단** → 끄기

---

## 5. 설치하기

여기까지 오면 **설치** 버튼이 있는 평범한 설치 화면이 나옵니다.
**설치** → 잠시 기다리면 → **열기** 가 뜹니다. **열기** 를 누릅니다.

> **이미 자동 스크롤이 깔려 있는데 "앱이 설치되지 않음" 이 뜬다면**
> 2026년 9월 12일 이전 버전이 서로 다른 키로 서명돼 있어서 그렇습니다.
> 설정 → 애플리케이션 → **자동 스크롤** → **삭제** 후 다시 설치하면 됩니다.
> 이 한 번만 지우면, 그 뒤로는 덮어 설치로 업데이트됩니다.

설치가 끝나고 앱을 열면 제목 아래에 **버전** 이 표시됩니다.
릴리스 페이지에 적힌 버전과 같으면 최신입니다.

---

## 6. "제한된 설정" 풀기 (가장 많이 막히는 곳)

앱을 열고 **접근성 서비스** 옆 `권한 주기` 를 누르면 접근성 목록이 나옵니다.
거기서 **자동 스크롤** 을 눌렀을 때 이런 창이 뜨면:

> 🛡️ **"앱의 액세스가 거부됨"**
> "이 제한된 권한 없이는 앱이 제대로 작동하지 않을 수 있습니다"

안드로이드 13부터, 스토어 밖에서 설치한 앱은 접근성을 기본으로 잠가둡니다.
**이 창에서는 아무리 눌러도 안 풀립니다.** 다른 화면에서 풀어야 합니다.

1. 창의 **닫기** 를 누릅니다
2. **설정 → 애플리케이션**(또는 앱)
3. 목록에서 **자동 스크롤** 을 누릅니다
4. 그 화면 **오른쪽 위 ⋮**(점 세 개)를 누릅니다
5. **제한된 설정 허용** 을 누릅니다
6. 지문이나 PIN을 물어보면 인증합니다

이제 다시 접근성으로 가면 스위치가 켜집니다.

> **⋮ 안에 "제한된 설정 허용" 이 안 보인다면** → 4번의 Play 프로텍트가 아직 켜져 있을 가능성이 큽니다. 껐는지 확인하고 2~6번을 다시 해보세요.

---

## 7. 권한 두 개 켜기

앱 화면 맨 위 **1. 준비하기** 칸의 두 항목이 모두 **"허용됨"** 이 되면 끝입니다.

### ① 접근성 서비스 — 화면을 대신 쓸어주는 권한

`권한 주기` → 설정 → 접근성 → **설치된 앱**(삼성은 *다운로드된 앱*) → **자동 스크롤** → 스위치 켜기 → 확인 → 뒤로

### ② 다른 앱 위에 표시 — 조작 바를 띄우는 권한

`권한 주기` → **자동 스크롤** → 스위치 켜기 → 뒤로

> 이 앱은 화면 내용을 읽거나 저장하지 않고, 인터넷도 쓰지 않습니다.
> 접근성 권한은 오직 "손가락 대신 화면을 쓸어주는" 데에만 씁니다.

---

## 8. 배터리 설정 (이걸 해야 중간에 안 멈춥니다)

안 하면 몇 분 뒤 폰이 앱을 재워버려서 스크롤이 혼자 멈춥니다.

설정 → 배터리 → **사용량 제한 앱**(또는 앱 절전) → 목록에 **자동 스크롤** 이 있으면 빼기

또는: 설정 → 애플리케이션 → **자동 스크롤** → **배터리** → **제한 없음**

---

## 다 됐습니다. 이제 써봅시다

1. 앱 맨 위 **시작** 을 누릅니다
2. 앱이 닫히고 작은 **조작 바** 가 뜹니다
3. **5초 안에 보상 앱을 엽니다**
4. 알아서 천천히 스크롤됩니다

### 처음엔 이 설정으로 시작해 보세요

| 항목 | 값 |
| --- | --- |
| 방향 | 왕복 |
| 아래로 가는 구간 | 40초 |
| 위로 가는 구간 | 20초 |
| 속도 | 2.5초 |
| 한 번에 넘기는 거리 | 25% |
| 쉬는 시간 | 1.5초 |
| 전체 실행 시간 | 보상 앱이 요구하는 시간 + 10초 |

### 조작 바 읽는 법

```
 ⠿   ↓   0:38   ☰   ▶   ✕
 │   │    │     │   │   └ 완전히 끝내기
 │   │    │     │   └ 시작 / 정지
 │   │    │     └ 가로 기준선 켜고 끄기
 │   │    └ 남은 시간
 │   └ 지금 가는 방향 (왕복이면 구간마다 뒤집힙니다)
 └ 여기를 끌어서 바를 옮깁니다
```

---

## 이런 화면이 뜨면

| 뜬 문구 | 해야 할 일 |
| --- | --- |
| "이런 유형의 파일은 기기에 해로울 수 있습니다" | **계속** 누르기 → [2번](#2-chrome-경고-넘기기) |
| "알 수 없는 앱을 설치할 수 없습니다" | [3번](#3-출처를-알-수-없는-앱-허용하기) |
| "기기 보호를 위해 앱 차단됨" | [4번](#4-google-play-프로텍트-차단-풀기) |
| "앱이 설치되지 않음" | [4번](#4-google-play-프로텍트-차단-풀기) (Play 프로텍트가 막은 것) |
| "앱의 액세스가 거부됨" / 접근성 스위치가 회색 | [6번](#6-제한된-설정-풀기-가장-많이-막히는-곳) |
| "패키지가 손상된 것 같습니다" | 파일을 다시 받아보세요. 받다가 끊긴 경우입니다 |

## 이런 증상이면

| 증상 | 해결 |
| --- | --- |
| 시작을 눌러도 화면이 안 움직임 | 접근성이 꺼졌습니다. [7번①](#-접근성-서비스--화면을-대신-쓸어주는-권한) 다시 확인 |
| 잘 되다가 몇 분 뒤 멈춤 | 배터리 절전입니다. [8번](#8-배터리-설정-이걸-해야-중간에-안-멈춥니다) |
| 너무 빨리 지나감 | 속도를 3초 이상, 거리를 20%로 낮추기 |
| 조작 바가 화면을 가림 | 왼쪽 ⠿ 를 끌어서 구석으로 옮기기 |
| 가로줄이 안 보임 | 설정의 **3. 가로 기준선** 에서 *한 줄* 또는 *두 줄* 선택 |
| 업데이트하고 싶음 | 같은 주소에서 새 APK를 받아 그냥 덮어 설치. 설정값은 유지됩니다 |
| "앱이 설치되지 않음" 인데 Play 프로텍트는 껐음 | 예전 버전이 다른 키로 서명돼 있을 수 있습니다. 설정 → 애플리케이션 → 자동 스크롤 → **삭제** 후 다시 설치하세요 (2026-09-12 이전 버전을 쓰셨다면 한 번은 필요합니다) |

---

## 친구에게 보내주기

앱 안 **4. 나눠주기** 에서:

- **설치 파일(APK) 보내기** — 지금 깔린 앱을 그대로 카카오톡·메일로 전송
- **앱 링크 공유** — 이 저장소 주소 공유

받는 사람도 **이 문서의 3 · 4 · 6번을 똑같이** 거쳐야 합니다.
이 문서 주소를 함께 보내주면 편합니다.

```
github.com/KwanghoJung01/Auto-scroll/blob/main/INSTALL.md
```

## 지우고 싶을 때

설정 → 애플리케이션 → **자동 스크롤** → **삭제**

앱을 지우면 권한도 함께 사라집니다.
설치할 때 껐던 **Play 프로텍트** 와 삼성 **자동 차단** 은 다시 켜 두세요.
